#include <bits/stdc++.h>
using namespace std;
vector<int>nge(5,-1);
//pse
void nget(vector<int>&arr,int n){
    stack<int>st;
    for(int i=0;i<n;i++){
        while(!st.empty() && st.top()>=arr[i]){
            st.pop();
        }
        if(i<n){
        if(st.empty()){
            nge[i]=-1;
        }
        else{
        nge[i]=st.top();
        }
        }
        st.push(arr[i]);
    }
}
int main(){
    vector<int>arr={1,5,2,4,3};
    int n=5;
    nget(arr,5);
    // int n=5;
    for(auto x:nge){
        cout<<x<<" ";
    }
    return 0;
}
